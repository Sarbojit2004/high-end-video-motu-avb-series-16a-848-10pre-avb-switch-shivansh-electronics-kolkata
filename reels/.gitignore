node_modules/
.cache/
.zipstage/
out/qa/
out/test/

# public/ is generated by `npm run setup` from the repository's own source
# assets — see README. Not tracked.
public/

# Everything in out/ is tracked: a 178 s reel encodes to ~31 MB and each
# uncompressed WAV is ~34 MB, all comfortably under GitHub'''s 100 MB file limit.
# (The long-form format needed workarounds here; at reel length nothing does.)
