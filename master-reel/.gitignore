node_modules/
.cache/
.zipstage/
out/qa/
out/test/

# public/ is generated by `npm run setup` from the repository's own source
# assets — see README. Not tracked.
public/

# Everything in out/ is tracked: this 298 s reel encodes to ~55 MB and each
# uncompressed WAV is ~57 MB, all comfortably under GitHub's 100 MB file limit.
# (The 898 s long-form format needed chunking here; at 298 s nothing does.)
build/
scripts/sheets/
scripts/inventory-raw.json
out/sheet-*.png
out/fix-check.png
out/solo-check.png
