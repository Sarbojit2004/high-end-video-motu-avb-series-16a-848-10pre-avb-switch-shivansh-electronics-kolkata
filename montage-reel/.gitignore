node_modules/
.cache/
build/
out/qa/
out/test/
out/*.log

# Generated by `npm run setup` from the repository's own source assets.
# Tracked: the open-licence font files, the committed audio analysis, and the
# tracked image-dimension manifest in src/data/.
public/images/
public/branding/
public/audio/*
!public/audio/analysis/
public/fonts/telegraf/

# Final renders: each part is < 100 MB by construction (README "Render + file
# size") and IS tracked, so the delivery lives in the repo. The music-only stem
# is derivable and not tracked.
out/motu-avb-montage-reel-music-only.wav
